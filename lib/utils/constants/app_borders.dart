import 'package:flutter/material.dart';

class AppBorders {
  static Border defaultBorder = Border.all(color: Colors.white);

  static BorderRadius defaultBorderRadius = BorderRadius.circular(10);
  static BorderRadius borderRadiusSmall = BorderRadius.circular(8);

  static Radius defaultRadius = const Radius.circular(10);
  static Radius radiusSmall = const Radius.circular(8);
}